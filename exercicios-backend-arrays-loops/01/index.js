const numeros = [2, 3, 4, 6];
let soma = 0
let conversor = 0

for (let soma = 0; soma < numeros.length; soma++) {
    conversor += numeros[soma]
} console.log(conversor)

// while (soma < numeros.length) {
//     conversor += numeros[soma]
//     soma++;
// } console.log(conversor)