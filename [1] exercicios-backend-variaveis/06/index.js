// velocidade = distancia/tempo * 3.6
// transformar metros por segundo para km multiplicando por 3.6

const distPercorrida = 1000 // metros
const tempo = 10 // segundos
const velocidade = (distPercorrida / tempo) * 3.6

console.log(`Média de ${velocidade}km/h`);

