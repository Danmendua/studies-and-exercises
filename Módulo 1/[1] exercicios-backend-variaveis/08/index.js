const R = 1 // valor do raio
const P = Math.PI; // valor de PI
const C = 2 * P * R // comprimento do círculo
const A = P * Math.pow(R, 2); // area do círculo
console.log(C.toFixed(3))
console.log(A.toFixed(3))